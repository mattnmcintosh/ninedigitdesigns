[build]
  command = "npm run build"
  publish = "dist"
  functions = "netlify/functions"

[dev]
  targetPort = 5173 # Tells Netlify Dev to wrap your Vite dev server

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200